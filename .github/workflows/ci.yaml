name: ci
on: push

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v7
    - name: Setup Go environment
      uses: actions/setup-go@v7
      with:
        go-version: 1.25
    - name: Run
      run: make test
